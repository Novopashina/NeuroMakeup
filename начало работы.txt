------------------сделано--------------------
АККУРАТНО удалить ненужные папки с сохраняемыми картинками (ВДРУГ будут ошибки, так что ПОСТОЯННО ПЕРЕЗАПУСКАЙ СЕРВЕР И ПРОВЕРЯЙ НАЛИЧИЕ ОШИБОК)
------------------сделано--------------------

В media/img загружаются 1-е фото без макияжа и 2-е фото с макияжем со своими изначальными названиями.
В media/processed_images загружается обработанное фото с названием processed_(какие-то буквы). ТАКЖЕ ОБАРБОТАННОЕ ФОТО СОХРАНЯЕТСЯ В БД, но НЕ В ВИДЕ КАРТИНКИ.

Подробно просмотреть и вспомнить, как работают представления, модели

Важно при переносе на amvera создать виртуальные окружения, куда и будут установлены все библиотеки.


Что будет, если в amvera нельзя публиковать сайты, использующие бд?
Скорее всего, нужно бы использовать более серьезную СУБД (Исправлено)

Как получится опубликовать все, пусть это будет один репозиторий на гитхабе.
Для диплома создать новый репозиторий и уже с него работать (Чтобы была рабочая версия).

НА ГИТХАБЕ ВСЕ В ОДНОМ РЕПОЗИТОРИИ ТОЛЬКО ДЛЯ ХРАНЕНИЯ, НО ДЛЯ РАБОТЫ ПРИЛОЖЕНИЯ В ОДНОЙ ПАПКЕ ОНИ НЕ МОГУТ НАХОДИТЬСЯ. У НЕЙРОСЕТИ СВОЕ ВИРТ.ОКРУЖЕНИЕ (установленное по пути C:/checkneuro/dmt - 2convenv), СОЗДАННОЕ С ПОМОЩЬЮ ANACONDA (Т.К. НЕКОТОРЫЕ БИБЛИОТЕКИ ДЛЯ РАБОТЫ НЕЙРОСЕТИ НЕ ХОТЕЛИ УСТАНАВЛИВАТЬСЯ Ч/З PIP INSTALL)

Пока что залила проект на гитхаб (для сохранности версии в ветку main). Далее нужно перенести с sqlite на postgres например для деплоя. (Исправлено)

!!!  Если для деплоя проекта в основе держать гитхаб, то там должна быть ветка MASTER, а сейчас там main.

!!! Для начала можно попробовать опубликовать на amvera только вирт.окружение с нейросетью и понять, как объединить с основным сайтом.

----------------------------------------------------------
17 сент. Базу данных отдельно как сервер публиковать не надо. В папку Data в amvera загрузить сам файл нужно будет.

Тек.деплой: 
1. Пока что все еще есть предупреждение:
WARN[0000] Failed to retrieve image docker.io/library/python:3.6-slim from remapped registry mirror.gcr.io: unable to complete operation after 0 attempts, last error: GET https://mirror.gcr.io/v2/token?scope=repository%3Alibrary%2Fpython%3Apull&service=: UNAUTHORIZED: authentication failed. Will try with the next registry, or fallback to the original registry.
Возможно из-за этого статус сборки: "Приложение запускается или работает с ошибкой"
2. Возможно, файл с сервером main3.py нужно слегка изменить (это к чатжпт)
---------------------------------------------------

21 декабрая. Перенос на БД Postgres успешен. Но хранятся только две фотографии, без обработанной. ОТМЕНА. ВСЕ СОХРАНЯЕТСЯ. ПРОСТО таблица main_myimage для хранения целевой и эталонной фотографий, а таблица main_processedimage для обарботанных (с датой создания)